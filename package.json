{
  "name": "Plex2Discord",
  "version": "0.0.3",
  "description": "a simple Node.JS script that listens for for Plex webhook payloads then formats and forwards the payload content to a set Discord channel via a bot.",
  "main": "Plex2Discord.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MIXERRULES",
  "license": "MIT",
  "dependencies": {
    "busboy": "^0.3.1",
    "discord.js": "^12.5.1",
    "express": "^4.17.1"
  }
}
